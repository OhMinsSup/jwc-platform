# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL is used to connect to your Supabase database.
# DATABASE_URL="postgres://postgres.[USERNAME]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?workaround=supabase-pooler.vercel"
# DATABASE -----------------------------------
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
DATABASE_ENGINE=postgres

# Nextjs -----------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_PAID_ACCOUNT_NUMBER=
NEXT_PUBLIC_SENTRY_DSN=

# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://next-auth.js.org/configuration/options#secret
# BetterAuth -----------------------------------------
PAYLOAD_PRIVATE_SECRET="ef9Zl7Uc9Yl5fKPZPcc8KUk4h1HR3nQsGD9Jsh1zgb41"
PAYLOAD_PRIVATE_REVALIDATION_KEY="payload-private-revalidation-key"

# Google -----------------------------------------
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_SHEET_ID=
GOOGLE_SHEET_TITLE=제목

# Secret -----------------------------------------
AES_KEY=

# Sentry -----------------------------------------
SENTRY_AUTH_TOKEN=
SENTRY_PROJECT=
SENTRY_ORG=
